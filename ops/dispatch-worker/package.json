{
  "name": "newsletter-dispatcher",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that dispatches the daily newsletter workflow on time (GitHub schedule: delivery is chronically delayed).",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
